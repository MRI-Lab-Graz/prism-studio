[mypy]
ignore_missing_imports = True
explicit_package_bases = True
# Keep broad `mypy .` aligned with repository policy: check only maintained
# runtime trees (`app/src` and `src`) and exclude everything else.
exclude = ^(?:\./)?(?!app/src/|src/).*