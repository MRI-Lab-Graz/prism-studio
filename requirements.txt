# Core dependencies
jsonschema
Pillow>=12.1.1
numpy
Nibabel
Flask>=3.1.3
Werkzeug>=3.1.6
bidsschematools>=0.8.0
requests>=2.25.0
bids-validator
pandas
openpyxl
waitress
pyreadstat
beautifulsoup4
colorama
cryptography>=46.0.5

# Note: tkinter (required for folder picker on Windows/Linux) is part of Python's
# standard library but requires system installation. Cannot be installed via pip.
# Windows: Ensure "tcl/tk and IDLE" is checked when installing Python
# Linux: Install python3-tk package (e.g., sudo apt-get install python3-tk)

# Documentation
sphinx
sphinx-rtd-theme
myst-parser

# Optional: EDF/EDF+ file support (requires C++ compiler on some systems)
# On Windows, pre-built wheels are usually available
# If installation fails, the tool will work without EDF metadata extraction
# pyedflib
defusedxml
pyarrow
