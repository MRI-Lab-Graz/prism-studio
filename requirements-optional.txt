# Optional feature dependencies not required for core app startup
# Install these when specific workflows require them.

# Neuroimaging tooling
Nibabel

# Optional export formats and physio support
pyedflib

# openMINDS metadata export (BIDS → openMINDS schema)
bids2openminds

# Questionnaire preview export as Word document
python-docx>=1.1.0

# Anatomical MRI defacing on export (invoked as the `pydeface` CLI via
# subprocess, not imported). Also requires FSL, a separate non-pip install,
# to actually run -- pointless without it, so it does not belong in the
# always-installed runtime set.
pydeface
